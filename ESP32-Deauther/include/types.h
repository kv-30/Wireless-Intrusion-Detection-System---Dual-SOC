#ifndef TYPES_H
#define TYPES_H

#include <stdint.h>

// Core logic removed for IP protection.
// This header preserves the public API and compilation dependencies.

typedef struct {
    uint8_t reserved[1];
} operation_frame_t;

#endif // TYPES_H
